Architecture de formatage devbox : source de vérité unique = .config/language/languages.lua (Lua pur, chargé par luajit standalone ET nvim via dofile) — M.formatters (cmd/args/config/write/exts) + M.languages (filetypes/treesitter/linter/formatter/lsp, lsp={name,config}). Consommé par nvim init.lua (dofile + conform prepend_args) et scripts/format.lua (luajit) : modes `format <dir>` (récursif via fd, .gitignore respecté, fonctionne hors dépôt) et `--staged` (format + re-stage pour le hook githooks/pre-commit, câblé via core.hooksPath githooks dans just setup) ; md5 avant/après pour rapporter modifiés vs scannés ; sorties formatters redirigées vers un log temporaire affiché seulement en échec ; exclut .config/language/ pour que biome ne formate pas son propre biome.json. Configs formatters dans .config/language/ (chemins absolus auto-résolus depuis l'emplacement du fichier) ; .editorconfig à la racine (spec EditorConfig). Formatters : biome.json, rustfmt.toml, ruff.toml, .clang-format, .taplo.toml, .stylua.toml — tous 2 espaces, max 120 chars. Fonction fish `format` → $HOME/scripts/format.lua ; recette just "format path". <!-- created=2026-08-30, last=2026-08-30 -->
§
Le script partagé scripts/open-in-herdr.sh (déplacé depuis .config/yazi/scripts/) ouvre des fichiers dans un nouvel onglet herdr via 'herdr tab create --cwd <dir> --label <basename> --focus --env HERDR_OPEN_FILE=<file>' ; config.fish lance $EDITOR au démarrage du shell (avant l'invite) pour éviter la course. Consommé par yazi (opener edit, chemins absolus) et sch (chemins relatifs résolus contre $PWD par le script). <!-- created=2026-08-30, last=2026-08-30 -->
§
Environnement : pi tourne dans la devbox, un conteneur podman/distrobox (Arch Linux) ; entrée kitty herdr, tout se passe dans le terminal. Home ~/.local/share/devbox-home = repo git versionné (MatteoBoursault/devbox), image des outils construite par Containerfile. Reproductibilité : toute modif outil/config/commande doit être renseignée dans Containerfile, justfile ou README.md (commandes) pour survivre à la recréation image/conteneur ; une modif faite à la main dans le conteneur est perdue. Déploiement : podman build puis distrobox create --home ~/.local/share/devbox-home (clés SSH et secrets hôte montés en lecture seule), init-hook = clone du repo + just setup. Pi : config ~/.config/pi (PI_CODING_AGENT_DIR dans config.fish) ; extensions npm dans .config/pi/settings.json : pi-hermes-memory, pi-subagents, pi-web-access, pi-mcp-adapter, rpiv-ask-user-question ; omp = autre harnais LLM (.config/omp, branché sur herdr). <!-- created=2026-08-30, last=2026-08-30 -->
§
Linters devbox : nvim-lint — non-stdin passent le nom de fichier (append_fname) ; stdin (luacheck, markdownlint) tournent avec le CWD de nvim → .luacheckrc et .markdownlint.json doivent être à la racine du repo. Configs : .luacheckrc racine (globals vim/ya/cx/Tab/Header/Status/ui, `_` pour discards ; W631 ignoré pour bookmarks.yazi — stylua: ignore) ; .markdownlint.json racine ; biome.json dans .config/language/. Biome 2.x : config nichée → 'biome lint' bare depuis la racine échoue ('Found a nested root configuration'), mais 'biome lint <file>' marche ; solution = biomejs overridé dans init.lua pour tourner depuis le dossier du buffer (CWD du fichier) ; vcs.useIgnoreFile → .gitignore respecté au bare lint ; --config-path = un répertoire (pas un fichier), rejette '..' ; configs renommées (biome.jsonc) ignorées. <!-- created=2026-08-30, last=2026-08-30 -->
§
Linter configs (luacheck globals, markdownlint rules) are now defined inside .config/language/languages.lua (single source of truth), with no root .luacheckrc or .markdownlint.json. nvim-lint invokes luacheck with --globals and markdownlint with --config .config/language/.markdownlint.json. This makes linting CWD-independent (works from /tmp). The biomejs linter in nvim-lint is overridden in init.lua to run from the buffer's directory to handle the nested biome.json. <!-- created=2026-08-30, last=2026-08-30 -->